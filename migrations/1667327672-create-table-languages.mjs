export async function up(sql) {
  await sql`
    CREATE TABLE languages (
      id integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
      language varchar(60) NOT NULL UNIQUE
    )
  `;
}

export async function down(sql) {
  await sql`
    DROP TABLE languages
  `;
}
